camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Demo
  filter:
    create: All tasks

server:
  port: 8085

spring:
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    root: INFO